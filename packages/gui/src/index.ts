/**
 * @perch/gui — the Electron app: a menu-bar (tray) entry that toggles a pinned,
 * always-on-top floating panel. The renderer subscribes to capability reads
 * over JSON-RPC and live-updates from the event bus.
 *
 * M5 adds Electron + the panel/menu-bar shell and the stack widget. Electron is
 * intentionally NOT a dependency yet (its binary download is deferred to M5).
 */
export const GUI_PLACEHOLDER = true;
